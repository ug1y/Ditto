image: python:3.10

stages:
  - setup
  - test

setup-job:
  stage: setup
  script:
    - pip install .[test] -i https://pypi.tuna.tsinghua.edu.cn/simple

test-job:
  stage: test
  script:
    - python -m pytest
  needs:
    - setup-job